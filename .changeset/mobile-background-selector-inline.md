---
"dopeshot-app": minor
---

Move background selector inline beneath canvas on mobile for easier access. The gradient picker is now visible directly beneath the size slider on mobile devices, while remaining in the Design bottom sheet for other controls. On desktop, the background selector stays in the sidebar as before.
