---
"dopeshot-app": minor
---

Add mesh and aurora gradient types to screenshot-derived gradients. Mesh gradients create organic, fluid blob shapes using multiple overlaid radial gradients. Aurora gradients produce an ethereal, flowing wave effect with layered linear gradients. The gradient picker now displays 6 gradient options in a compact 2x3 grid layout.
