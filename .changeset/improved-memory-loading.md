---
"dopeshot-app": patch
---

Improve memory sidebar loading experience with pre-loading and subtle skeleton states. Remove duplicate fetch logic, replace spinner with skeleton loaders, and add save count to Saved button (e.g., "Saved (1/5)").
