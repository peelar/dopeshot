---
"dopeshot-app": patch
---

Update mobile sticky gradient selector to show 4 gradients (3 linear + mesh) in a single row. The mobile inline variant displays the most commonly used gradients in a compact 4-column layout, while the full Design sheet shows all 6 gradients including ambient options.
